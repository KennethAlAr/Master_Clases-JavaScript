// ->          ||  ->  ó  -> OR
// ->          &&  ->  y  -> AND
// ->          !   ->  no -> NOT
// ->          ==  -> Igual que
// ->          === -> Estrictamente igual que
// ->          !=  -> Desigual
// ->          !== -> Estrictamente desigual
// ->          =   -> Operador de asignación
// ->          <   -> Menor que
// ->          <=  -> Menor o igual que
// ->          >   -> Mayor que
// ->          >=  -> Mayor o igual que

//La diferencia entre "igual que" y "estrictamente igual que" es que el "igual que" solo tiene en cuenta el contendio, el "estrictamente igual que" tiene en cuenta también el tipo de dato.