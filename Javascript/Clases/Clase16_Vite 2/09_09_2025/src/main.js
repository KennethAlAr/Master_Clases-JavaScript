import './style.css'
import {movies as peliculas, shows as series, hello as hola} from "./data.js";

//con el XXXXXX as YYYYYY importamos modulos con alias, para utilizar el nombre que queramos en vez del que viene por defecto.

console.log(peliculas);
console.log(series);
hola();